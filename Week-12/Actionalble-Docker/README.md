## Common Docker Commands
    - docker run
        eg: docker run mongo
            docker run -p 27017:27017 mongo
    - docker ps
        - displays all the containers running in the background
    - docker kill
        - docker kill <container_id>: Will stop the container that you are running